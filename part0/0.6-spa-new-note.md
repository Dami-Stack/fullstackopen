sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST /exampleapp/new_note_spa
    Note right of browser: Note sent as JSON
    server-->>browser: 201 Created

    Note right of browser: Page is not reloaded, UI updates instantly
